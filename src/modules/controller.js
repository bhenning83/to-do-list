const controller = (() => {
  const allTasks = [];
  const allProjects = [];

  const form = document.getElementById("project-form");

  function createTask() {
    
  }

  return { allTasks, allProjects }
})();

export default controller;